import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Header from "@/components/layout/header"
import Footer from "@/components/layout/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Indus Wealth Management - Simplifying Wealth, Amplifying Growth",
  description:
    "Your one-stop solution for financial planning, investments, and wealth management services. Build a secure financial future with expert guidance.",
  keywords:
    "wealth management, financial planning, mutual funds, insurance, retirement planning, tax advisory, investment advisory, India",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
